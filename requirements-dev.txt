# Additionally needed for testing and packaging.
#   .venv/bin/pip install -r requirements-dev.txt
-r requirements.txt
pytest==9.1.1
pyinstaller==6.22.3
# Lint, settings in pyproject.toml: .venv/bin/ruff check .
ruff==0.16.8
